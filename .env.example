# 对外端口（前端 + 反代 API）
PORT=8080

# 管理口令 / JWT（仅这两项必须用环境变量）
ADMIN_PASSWORD=change-me-admin
JWT_SECRET=please-use-a-long-random-string

# 上游超时（秒）；Base URL / API Key / Model 在管理后台配置
UPSTREAM_TIMEOUT_SECONDS=300

# 可选
# CORS_ORIGINS=*
